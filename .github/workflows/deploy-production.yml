name: Deploy to Production
run-name: ${{ github.actor }} is deploying to production

on:
  push:
    branches: [ 116-refer-producció ]

jobs:
  Stop-docker-compose:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: conexió al servidor i parada de docker compose
        run: |
          echo "${{ secrets.PROD_SECRET_KEY }}" > ~/prod_key.pem
          chmod 600 ~/prod_key.pem

          ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -i ~/prod_key.pem ${{ secrets.PROD_USERNAME }}@${{ secrets.PROD_HOST }} "cd conexus && docker compose down"
      - run: echo "🍏 This job's status is ${{ job.status }}."
          
  